[project]
name = "billstojson"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "abdulvajid1", email = "vajidca444@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
billstojson = "billstojson:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
